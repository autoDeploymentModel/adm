# Plan: Git Commit for Sampling Parameter Updates

## Changes Summary
Modified `src/settings.html` with the following changes:

### 1. Preset mode parameter adjustments
- **default**: Added `ctx_size: 4096`, adjusted temperature (0.7→1.0), top_k (40→20), repeat_penalty (1.1→1.0), dry_multiplier (0.8→0.0), presence_penalty (0.0→1.5)
- **creative**: Added `ctx_size: 32768`, adjusted temperature (0.85→1.0), top_k (40→20), repeat_penalty (1.05→1.0), dry_multiplier (0.4→0.0), dry_allowed_length (3→2), presence_penalty (0.0→1.5), reasoning (off→auto)
- **code**: Added `ctx_size: 128000`, adjusted temperature (0.2→0.6), top_k (35→20), top_p (0.9→0.95), repeat_penalty (1.1→1.0), dry_multiplier (0.4→0.0)

### 2. Default value updates
- temperature default: 0.8/0.2 → 1.0
- top_k default: 40 → 20
- repeat_penalty default: 1.1 → 1.0
- dry_multiplier default: 0.8 → 0.0
- presence_penalty default: 0.0 → 1.5
- ctx_size default: 9096 → 4096 (typo fix)
- min_p default: 0.05 → 0.0

### 3. Bug fix
- `ctx_size` in resetParams was `9096`, corrected to `4096`

## Commit Message
```
fix(settings): 优化采样参数预设值，修复ctx_size默认值拼写错误
```

## Steps
1. `git add src/settings.html`
2. `git commit -m "fix(settings): 优化采样参数预设值，修复ctx_size默认值拼写错误"`
