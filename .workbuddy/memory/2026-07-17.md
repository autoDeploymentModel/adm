# 2026-07-17

## 功能：Agent 页「添加云端模型」
- 在 `src/agent.html` 顶部栏新增「添加云端模型」按钮，弹出模态表单（模型名称 / Base URL / API Key / 上下文大小，默认 256000 即 256K，支持 256K、1M 写法）。
- 新增 Rust 命令 `add_cloud_provider`（`src-tauri/src/pages/agent.rs`），写入 `C:\Users\{用户}\.config\admAgent\admAgent.json` 的 `providers` 分支（type=openai-compat，含 models 数组）。
  - 调用前先 `ensure_adm_agent_config` 保证文件含合法 `providers.local`，避免后续启动 Agent 时被默认结构覆盖云端 provider。
  - provider key 与 model id 由模型名称派生（slugify）；原子写入（tmp+rename）。
  - K=1000、M=1000000（与示例 256K=256000 一致）。
- 已在 `lib.rs` 注册命令；`cargo check` 通过。

## 功能扩展：模型管理（列表 + 编辑）
- 顶部栏新增「模型管理」按钮，弹窗列出所有已添加云端模型（排除自动管理的 `local`），每项带「编辑」按钮。
- 新增 Rust 命令：`list_cloud_providers`（返回 key/name/base_url/api_key/context_window）、`update_cloud_provider(key, input)`（按 key 整体更新，名称变更时重派生 model id，保留同一 key 不产生孤儿）。均已在 `lib.rs` 注册。
- 编辑复用「添加云端模型」同一表单：通过 `editingKey` 区分新增/编辑模式，列表数据缓存于 `cloudProviderCache` 供回填；保存后若管理弹窗仍打开则自动刷新列表。
- 同步更新 `doc/dev_doc.md` 的命令表与说明。
- `cargo check` 通过（原 `app` 未使用参数改为 `_app` 消除告警）。

## 修复：model id 与 name 不一致
- `slugify_model_id` 之前把 `.` 当标点丢弃，导致 `mimo-v2.5` → `mimo-v25` 而 name 仍为 `mimo-v2.5`。已改为保留点号（与原始示例 `id:"mimo-v2.5"` 一致），并去掉首尾多余的 `-`/`.`。`cargo check` 通过。

## 功能：添加/修改模型后自动重启 Agent 终端
- `agent.html` 新增 `restartAgentTerminal()`：先 `get_agent_status` 判断是否运行，运行中则 `stop_agent_terminal` 后 `startAgentNow()` 重新拉起 admAgent 以加载最新 `admAgent.json`。
- `confirmCloudModel` 成功回调（1.4s 后）改为 `closeCloudDialog()` + `closeManageDialog()` + `restartAgentTerminal()`，并调整成功文案为「（新配置将在 Agent 终端重启后生效）」。
- 仅前端改动；复用已有 `stop_agent_terminal` / `start_agent_terminal` 命令，无需改后端。JS 语法已用 node 校验通过。

## 修复：确认按钮重复点击
- `confirmCloudModel` 新增 `cloudSubmitting` 防重入标记：点击后立即禁用「确认/取消」按钮（`setCloudButtonsDisabled`），校验失败或保存失败时 `finishCloudSubmit()` 恢复以允许重试；保存成功则保持禁用直至窗口关闭。
- 成功后的关闭延时由 1400ms 缩短为 600ms，窗口更快关闭。
- `openCloudDialog` / `openCloudEdit` 打开时重置 `cloudSubmitting` 并恢复按钮可用；新增 `.cloud-actions button:disabled` 置灰样式。
- 根因：异步保存期间及 1.4s 延时内按钮仍可点击，导致重复提交。JS 语法已用 node 校验通过。
