# 2026-07-28

- 审核当前未提交的 Agent 页改动（Todo 固定面板及云端 provider 运行时同步/自愈逻辑），仅审查未改业务代码。确认 `pnpm typecheck` 与 `git diff --check` 通过；发现两项中等风险：Todo 列表被清空时 SSE 因 `omitempty` 不刷新导致旧面板残留，以及添加/删除 provider 后未刷新 `S.serverProviders` 导致下拉列表显示过期模型并可再次选中。
- 已修复审核发现的三项问题：添加/删除 provider 后刷新服务端 provider 快照；Session SSE 将缺失的 `todos` 作为空列表并同步 `S.currentConv`；workspace 切换时同步清空 Todo 面板。更新 `doc/dev_log.md` 3.28；`pnpm typecheck` 与 `git diff --check` 均通过。
- 审核 `HEAD~10..HEAD` 最近 10 次提交，未修改业务代码。确认 4 项问题：旧路由 loading timer 可卸载新视图并永久停在 loading；Agent `initSeq` 无法阻止并发 `start_agent_server` 的进程副作用，可能产生孤儿 admAgent；发送安全计时器使用当前 UI 会话而非运行会话；新增 provider 的运行时同步失败被吞掉却仍提示成功并暴露不可用模型。`pnpm typecheck` 和 Tauri debug no-bundle 构建通过；累计 diff 仅有 `src/views/agent.js` EOF 多余空行。
- 已修复上述 4 项问题并更新 `doc/dev_log.md` 3.29：路由 loading 回调校验代次；Rust 使用异步单飞锁串行化 admAgent 启动/停止/下载/升级并为启动失败启用子进程自动清理；前端以 `activeRun` 固定 workspace/session/runId，统一安全超时、取消、SSE 续期和完成事件过滤；provider 只有经 `/config/set` 与 `/providers` 双重确认才提示成功并进入下拉。`pnpm typecheck`、`cargo check`、`git diff --check` 和 Tauri debug no-bundle 构建均通过。全仓 `cargo fmt --check` 因大量既有 Rust 格式差异未通过，本次未批量格式化以避免无关改动。
